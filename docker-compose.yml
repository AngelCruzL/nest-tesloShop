version: '3'

services:
  app:
    container_name: tesloapp
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    ports:
      - 3000:3000
    volumes:
      - ./src:/app/src
